# FAQ（常见问题）

## 连接类

**Q：手机连不上（连接失败 / 超时）？**
1. 确认电脑端 dsh 已启动、`webserver` 配置了 `host: 0.0.0.0`（默认 127.0.0.1 只允许本机）
2. 手机和电脑必须在同一网络（同一 WiFi / Tailscale）
3. 口令是否正确（`cordis.patch.yml` 的 `authToken`）；改了口令要重启桌面端
4. Windows 防火墙若拦截 3080，放行 `node`/`DeepSeek Harness`

**Q：扫码连不上 / 二维码扫了没反应？**
- App 请用自带的「扫码连接」按钮，不要用系统相机（系统相机会跳浏览器打开网页）
- 二维码含口令，改了 `authToken` 后需重启桌面端，二维码自动更新
- 手机与电脑需同一网络

**Q：外出（不在家）怎么连？**
Tailscale 组网：电脑和手机装 Tailscale 登录同一账号，手机访问 `http://<电脑Tailscale IP>:3080/m`。禁止公网端口映射。

## 使用类

**Q：发消息没反应？**
- agent 正在处理上一轮时新消息会**排队**（dsh 机制），移动端会提示；等上一轮完成即可
- 检查状态点：绿色=运行中，橙色=等待你回答（直接回复即可）

**Q：通知不推送？**
- 检查 `pushUrls` 配置是否正确 + 重启桌面端
- Server酱：SendKey 是否有效；ntfy：手机 App 是否订阅了同一 topic
- 同会话同类型通知 60 秒内合并（`pushCooldownMs`）

**Q：通知列表里看不到任务完成？**
通知中心按会话聚合（同会话同类型只保留一条）；PC 端任务完成/失败/需回答才会产生通知。

**Q：新建会话选的工作目录在 PC 端显示"未分组"？**
该目录不在任何已注册工作区（或不在其子目录下）。在工作区子目录下会自动归属；也可在 PC 端把该目录注册为工作区。

**Q：会话列表和 PC 端不一致？**
不会 —— App/网页端与 PC 端共用同一会话数据源（实时读写），标题/分组/归档全部同源。

## 深色/显示类

**Q：界面是黑的 / 想换浅色？**
设置 → 深色模式：跟随系统 / 深色 / 浅色 三态切换（App）；网页端跟随浏览器系统主题。

**Q：看不到工具调用过程？**
移动端默认隐藏（只显示结果）；设置 →「显示工具调用」打开后显示折叠卡。

## 安全类

**Q：二维码/口令泄露了怎么办？**
修改 `cordis.patch.yml` 的 `authToken` → 重启桌面端 → 二维码自动更新，旧码作废。

**Q：可以把 3080 映射到公网吗？**
**禁止**。插件面向可信内网设计（详见 docs/04-security.md）；外出请用 Tailscale。

**Q：App 会联网上传我的数据吗？**
不会。App 只与本机配置的电脑地址通信（HTTP 直连），连接信息仅存手机本地。
